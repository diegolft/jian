// The generated client and types are produced at build time from openapi.json by the
// OpenAPIGenerator plugin. This file exists so SwiftPM recognises the target: a target
// whose only sources come from a plugin is dropped from the package graph.
